#!/usr/bin/env bash

echo "==> Homebrew cleanup..."
brew cleanup

echo "==> Docker cleanup..."
docker system prune --all --force

echo "==> uv cleanup..."
uv cache prune

echo "==> mise cleanup..."
mise prune -y

echo "==> ~/tmp cleanup..."
rm -rf ~/tmp/*

echo "==> Done."
