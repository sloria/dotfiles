#!/usr/bin/env bash

echo "==> Homebrew cleanup..."
brew cleanup

echo "==> Docker cleanup..."
docker system prune --all --force

echo "==> uv cleanup..."
uv cache prune

echo "==> ~/tmp cleanup..."
rm -rf ~/tmp/*

echo "==> Done."
